# Marketing site — civiumcre.com

Source: `index.html` in https://github.com/ccrouch-cyber/civiumcre-site (served by GitHub Pages from `main`).

The site today is a single holding page; `index.html` here reproduces it exactly, with its local :root replaced by the system's tokens (identical values). The designed pages have not landed yet, so no other site screens exist to recreate. Holding-page type differs slightly from the product: body 15/1.5, brand mark 32px with 9px tracking, measure 720px, 96px top padding.
