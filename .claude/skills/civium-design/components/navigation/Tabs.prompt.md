One row of serif tabs per page — the lanes of a deal; the active one in ink with a 2px gold underline.

```jsx
<Tabs items={['Rent Roll Analyzer','T-12 Analyzer','Market','Underwriting','The deal in one page']}
  active={lane} onChange={setLane} />
```
