One style for text inputs and selects; label left in body, unit right in tiny fade.

```jsx
<Input defaultValue="x@x.test" aria-label="email" />
<Input state="bad" defaultValue="short" />
<Input state="busy" defaultValue="reading…" />
<Input as="select" options={['add to Larkspur Crossing']} />
<Input label="vacancy" unit="%" defaultValue="5.0" />
```
