The surface every block sits on: card color, 1px line border, 10px radius, shadow-1; `clickable` lifts on hover.

```jsx
<Card eyebrow="Drop a deal" eyebrowGold hint="it reads on the server and lands as a deal when it is ready">
  <Button>…</Button>
</Card>
```

- Panels inside a card: `wash` ground, `line` border, `radius-panel` (8px). Drop zone: 1px dashed `faint`.
